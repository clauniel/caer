IF (NOT SYNAPSERECONFIG)
	SET(SYNAPSERECONFIG 0 CACHE BOOL "Enable DAVIS 240C to Dynapse FPGA mapping.")
ENDIF()

IF (SYNAPSERECONFIG)
    ADD_LIBRARY(synapsereconfig SHARED synapsereconfig.c)

    TARGET_LINK_LIBRARIES(synapsereconfig ${CAER_C_LIBS})

ENDIF()

